import React from "react";

const Empty = ({ page }) => {
  return (
    <p style={{ textAlign: "center", color: `#fff`, fontSize: "2.5rem" }}>
      {page} Empty
    </p>
  );
};

export default Empty;
